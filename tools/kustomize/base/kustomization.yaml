apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 为所有资源添加公共标签和注解（所有环境共享）
labels:
- includeSelectors: true
  pairs:
    managed-by: kustomize

commonAnnotations:
  note: generated by kustomize

# 引用的资源文件
resources:
- deployment.yaml
- service.yaml
